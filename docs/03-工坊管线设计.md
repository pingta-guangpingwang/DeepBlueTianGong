# 工坊管线设计 — 从驾驭工程提取

## 一、提取清单

以下模块从 `DeepBlueGodHarnessFarm` 提取，**零逻辑改动**，仅调整 import 路径。

| # | 源文件 | 目标文件 | 代码行数 | 改动 |
|---|--------|----------|----------|------|
| 1 | `electron/modules/resourceStore.ts` | `electron/core/resourceStore.ts` | ~1000 | `REPO_NAMES` → `repoConfig.getRepos()` |
| 2 | `electron/modules/pendingResourceStore.ts` | `electron/core/pendingResourceStore.ts` | ~135 | 无 |
| 3 | `electron/modules/userContributionStore.ts` | `electron/core/userContributionStore.ts` | ~123 | 无 |
| 4 | `electron/modules/database.ts` | `electron/core/database.ts` | ~78 | 简化（去 HubSettings/ProjectMetadata） |

---

## 二、resourceStore 改造明细

### 2.1 REPO_NAMES 动态化

**改前**（驾驭工程）：
```typescript
const REPO_NAMES: RepoName[] = ['DeepBluePrompt', 'DeepBlueCase', 'DeepBlueKit', 'DeepBlueIdentity']
export type RepoName = 'DeepBluePrompt' | 'DeepBlueCase' | 'DeepBlueKit' | 'DeepBlueIdentity'
```

**改后**（天工）：
```typescript
import { repoConfig } from '../config/repoConfig'

// 不再有 RepoName 类型，统一用 string
function getRepoList(): string[] {
  return repoConfig.getRepos().map(r => r.name)
}
```

所有使用 `REPO_NAMES` 的地方改为 `getRepoList()`。

### 2.2 目录映射动态化

**改前**：
```typescript
getTypeDir(repo: RepoName, type: string): string {
  if (repo === 'DeepBluePrompt') return 'prompts'
  if (repo === 'DeepBlueCase') return type === 'template' ? 'templates' : 'cases'
  if (repo === 'DeepBlueKit') {
    if (type === 'plugin') return 'plugins'
    // ... 7 层 if-else
  }
  return 'other'
}
```

**改后**：
```typescript
getTypeDir(repo: string, type: string): string {
  const cfg = repoConfig.getRepo(repo)
  const typeDef = cfg?.taxonomy.types.find(t => t.id === type)
  return typeDef?.dir || 'other'
}
```

### 2.3 YAML 校验动态化

**改前**：
```typescript
const REQUIRED_FIELDS = ['id', 'name', 'type', 'category', 'summary']
const VALID_TYPES = ['prompt', 'template', 'case', 'plugin', 'tool', ...]
```

**改后**：
```typescript
const cfg = repoConfig.getRepo(repo)
const REQUIRED_FIELDS = cfg?.taxonomy.requiredFields || ['id', 'name', 'type', 'category', 'summary']
const VALID_TYPES = cfg?.taxonomy.types.map(t => t.id) || []
```

### 2.4 其他方法不受影响

以下方法完全不变，直接复用：

```typescript
// 查询组（不变）
queryResources(params)
filterResources(filters)
getResourceDetail(id)
getAllResources(repo?)
calcRelevance(item, query)
matchesFilters(item, params)
toResourceItem(item, repo)

// Git 操作组（不变）
git(repo, args)
gitSilent(repo, args)
isGitRepo(repo)
getRepoStatus(repo)
cloneRepo(repo, remoteUrl?)
autoSyncAll()
forcePullOrAbort(repo)
getLocalChanges(repo)         // 含缓存

// 提交组（不变）
getNewFilesOnly(repo)
validateNewFileYaml(repo, filePath)
validateCommitScope(repo)
commitChanges(repo, message, files?)
pushBranch(repo, branchName)
safePushBranch(repo, branchName)

// 评分组（不变）
rateResource(id, score)
getLeaderboard(category?, limit?)

// 辅助（不变）
isInitialized()
ensureManifestsLoaded()
loadManifests()
isGhAvailable()
isGitHubAuthenticated()
createPullRequest(...)
```

---

## 三、仓库配置模块（全新）

### repoConfig.ts

```typescript
// electron/config/repoConfig.ts

interface RepoTaxonomy {
  types: Array<{ id: string; label: string; dir: string }>
  categories: string[]
  requiredFields: string[]
  optionalFields?: string[]
}

interface RepoConfig {
  name: string
  label: string
  labelEn?: string
  remote: string
  visible: boolean
  taxonomy: RepoTaxonomy
}

interface AppConfig {
  version: string
  repos: RepoConfig[]
}

class RepoConfigStore {
  private config: AppConfig
  private configPath: string

  constructor() {
    this.configPath = this.getConfigPath()
    this.config = this.load()
  }

  getRepos(): RepoConfig[] { return this.config.repos }
  getVisibleRepos(): RepoConfig[] { return this.config.repos.filter(r => r.visible) }
  getRepo(name: string): RepoConfig | undefined { return this.config.repos.find(r => r.name === name) }
  getTaxonomy(repo: string): RepoTaxonomy | undefined { return this.getRepo(repo)?.taxonomy }

  // 动态添加仓库
  addRepo(repo: RepoConfig): void { this.config.repos.push(repo); this.save() }
  removeRepo(name: string): boolean { /* filter + save */ }

  private getConfigPath(): string {
    const appData = process.env.APPDATA || process.env.HOME || '.'
    const dir = path.join(appData, 'dbghf')
    if (!fs.existsSync(dir)) fs.mkdirSync(dir, { recursive: true })
    return path.join(dir, 'repos.json')
  }

  private load(): AppConfig { /* 读 JSON */ }
  private save(): void { /* 原子写 */ }
}

export const repoConfig = new RepoConfigStore()
```

---

## 四、采集源配置模块（全新）

### sourceConfig.ts

```typescript
// electron/config/sourceConfig.ts

interface SourceConfig {
  id: string
  name: string
  targetRepo: string
  prompt: string            // Claude Code 采集提示词
  schedule: string          // cron 或 'manual'
  parallelism: number       // 同时运行的 Claude 进程数 (1-4)
  batchSize: number         // 每个进程处理的条目数 (5-50)
  enabled: boolean
  autoSubmit: boolean       // 采集后自动审核+提交
}

class SourceConfigStore {
  private sources: SourceConfig[]

  getSources(): SourceConfig[]
  getSource(id: string): SourceConfig | undefined
  addSource(s: SourceConfig): void
  removeSource(id: string): boolean
  // load/save JSON
}

export const sourceConfig = new SourceConfigStore()
```

---

## 五、contribution 管线独立化

现有 `harnessIpc.ts` 中的 `contribution:commit-all` 逻辑抽离为纯函数：

```typescript
// electron/core/contributionPipeline.ts

interface PipelineResult {
  repo: string
  success: boolean
  message: string
  step?: 'scope-check' | 'pull' | 'commit' | 'push'
}

async function commitAllRepos(message: string): Promise<{ success: boolean; results: PipelineResult[] }> {
  const repos = repoConfig.getVisibleRepos()
  const results: PipelineResult[] = []

  for (const repo of repos) {
    // Step 1: 检查变更
    const changes = await resourceStore.getLocalChanges(repo.name)
    if (changes.length === 0) {
      results.push({ repo: repo.name, success: true, message: '无变更，跳过' })
      continue
    }

    // Step 2: 闸门 1 - 范围管控 + YAML 校验
    const scopeCheck = await resourceStore.validateCommitScope(repo.name)
    if (!scopeCheck.valid) {
      results.push({ repo: repo.name, success: false, message: scopeCheck.errors.join('\n'), step: 'scope-check' })
      continue
    }

    // Step 3: 闸门 2 - 强制拉取
    const pullResult = await resourceStore.forcePullOrAbort(repo.name)
    if (!pullResult.success) {
      results.push({ repo: repo.name, success: false, message: pullResult.message, step: 'pull' })
      continue
    }

    // Step 4: 精确提交
    const newFilePaths = scopeCheck.newFiles.map(f => f.path)
    const commitResult = await resourceStore.commitChanges(repo.name, message, newFilePaths)
    if (!commitResult.success) {
      results.push({ repo: repo.name, success: false, message: commitResult.message, step: 'commit' })
      continue
    }

    // Step 5: 闸门 3 - 安全推送
    const status = await resourceStore.getRepoStatus(repo.name).catch(() => null)
    const branch = status?.branch || 'main'
    const pushResult = await resourceStore.safePushBranch(repo.name, branch)
    if (pushResult.success) {
      // 标记贡献状态
      const contribs = userContributionStore.contributions.filter(c => c.repo === repo.name && !c.committed)
      if (contribs.length > 0) {
        userContributionStore.markCommitted(contribs.map(c => c.id))
        userContributionStore.markPushed(contribs.map(c => c.id))
      }
      results.push({ repo: repo.name, success: true, message: `已提交并推送` })
    } else {
      results.push({ repo: repo.name, success: false, message: pushResult.message, step: 'push' })
    }
  }
  return { success: results.every(r => r.success), results }
}
```

---

## 六、IPC 接口定义

天工的 IPC 接口比驾驭工程精简得多，只保留工坊核心 + 采集控制：

```typescript
// 资源工坊（从驾驭工程提取，接口不变）
resource:status
resource:query
resource:list
resource:detail
resource:leaderboard
resource:changes
resource:repo-status

// Git 同步与贡献
resource:auto-sync
resource:sync-pull
resource:check-updates
resource:clone

// 待审核管理
pending:list
pending:add
pending:remove
pending:approve
pending:approve-all
pending:audit-all
pending:count
pending:cleanup

// 贡献追踪
contribution:list
contribution:check-status
contribution:commit-all

// 采集控制（全新）
collector:run            → 启动采集
collector:abort          → 中止采集
collector:status         → 查询采集状态
collector:history        → 采集历史
collector:source-config  → 读数据源配置
collector:save-source    → 保存数据源配置

// 仓库配置（全新）
repo:list              → 列出所有仓库配置
repo:add               → 添加仓库
repo:remove            → 移除仓库
repo:update            → 更新仓库配置
```

**不提取的**（驾驭工程专属）：
- `harness:*` — 驾驭智能体
- `cli:*` — CLI 命令系统
- `plugin:*` — 插件系统
- `workflow:*` — 工作流
- `system:*` — 规则/审计/性能
- `hub:*` — 托盘/热键/悬浮窗
- `horseFarm:*` — 项目农场
- `sandbox:*` — 沙盒
- `session:*` — 会话管理
- `pty:*` — PTY 终端

---

## 七、前后端数据流

```
React 前端                         Electron 主进程
┌──────────────┐                  ┌──────────────────────┐
│ Dashboard    │── collector:run ─→│ collectorEngine       │
│              │                  │   ├─ claudeRunner × N │
│              │← collector:event─│   └─ outputParser     │
│              │                  │        ↓              │
│              │                  │ pendingResourceStore  │
├──────────────┤                  ├──────────────────────┤
│ Resource     │── resource:list─→│ resourceStore         │
│ Market       │                  │   ↓                   │
│              │← resources ─────│ aiIndexes (内存缓存)    │
├──────────────┤                  ├──────────────────────┤
│ PendingPanel │── pending:list──→│ pendingResourceStore  │
│              │── audit-all ────→│   ↓                   │
│              │                  │ resourceStore.addRes  │
├──────────────┤                  ├──────────────────────┤
│ CommitPanel  │── commit-all ───→│ contributionPipeline  │
│              │← results ───────│   ↓                   │
│              │                  │ GitHub push           │
└──────────────┘                  └──────────────────────┘
```
